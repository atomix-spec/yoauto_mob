import '../models/config_models.dart';

/// Config service - placeholder, API does not expose a config endpoint yet.
class ConfigService {
  Future<MobileConfig> getConfig() async => MobileConfig();
}
